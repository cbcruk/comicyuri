# cooViewer에서 가져올 것

레퍼런스로 삼은 cooViewer를 읽고, comicyuri에 아직 없는 동작을 할 순서대로 적은
문서입니다.

## 이 문서에 대하여

`SPEC.md`는 **지금 되는 것**을 적습니다. 이 문서는 **아직 없는 것**을 적습니다.
항목을 구현하면 `SPEC.md`로 옮기고 여기서는 지웁니다. 스프레드 묶기를 다루던
1장이 그렇게 비어 `S-121`·`R-226`·`R-227`로 갔고, `C-208`과 `C-301`이 함께
`R-216`으로, `C-303`이 `R-2B3`으로, `C-201`과 `C-202`가 함께
`R-240`·`R-246`·`R-247`로, `C-203`이 `R-228`로, `C-204`가 `R-2B4`로, `C-205`가 `R-229`로, `C-206`이 `R-2A5`로, `C-209`가
`R-217`로, `C-207`과 `C-210`이 `R-266`과 `R-2C1`로, `C-304`가 `R-2B5`로, `C-306`이
`R-286`으로 갔습니다. 2장과 3장은 이것으로 비었습니다. 장 번호는 그대로 둡니다 —
항목 번호가 그것을 따라가므로, 다시 매기면 `C-401`이 4장에 없는 문서가 됩니다.

**가져오면서 줄인 것.** `C-208`의 네 선택지 중 "다음 책의 마지막 페이지로"는
빼고 셋으로 만들었습니다. 이 뷰어에서 책은 언제나 저장된 위치에서 열리므로
(`N-403`), 이웃한 책도 그렇게 여는 것이 앞뒤가 맞습니다. 다 읽은 앞 권으로
돌아가면 그 책의 끝이 나오고, 안 읽은 다음 권을 열면 첫 장이 나옵니다 — 두
선택지가 필요했던 자리를 저장된 위치가 이미 메웁니다.

`C-201`의 네 가지 휠 동작도 하나로 줄였습니다. 굴려서 갈 곳이 있으면 굴리고,
끝에 닿아 있으면 넘깁니다 — 화면에 통째로 들어가는 페이지는 처음부터 끝에 닿아
있으므로 "항상 넘긴다"가 저절로 따라옵니다. 고를 것이 없으니 설정도 없습니다.
대신 장치가 고릅니다. 넘기는 것은 마우스 휠뿐이고, 트랙패드는 끝에서 멈춥니다
(`R-246`) — 원본에는 트랙패드가 없었습니다.

`C-207`에서 퍼센트로 가는 길은 빼고 번호만 가져왔습니다(`R-266`). 비율로 잡는
자리는 슬라이더가 이미 하고 있어서, 같은 일을 하는 입력란을 하나 더 두는 셈이었습니다.

`C-209`의 글꼴·색 설정과 자동 숨김은 가져오지 않았습니다(`R-217`). 이름은 카운터
아래 한 줄이고 툴바와 함께 숨으므로, 따로 숨길 것도 꾸밀 것도 없습니다. 원본이
`(67.jpg | 89.jpg)`로 붙이던 자리를 여기서는 꼬리만 남기고 앞을 줄입니다.

`C-206`에서는 건너뛰기만 가져왔습니다(`R-2A5`). 반 페이지 앞뒤로 가는 것은 굴려서
읽는 것(`R-240`·`R-246`)이 이미 대신합니다. 건너뛸 장수도 고르게 하지 않고 열 장으로
두었습니다 — 크게 움직이는 다른 길이 이미 슬라이더와 썸네일 격자로 둘 있습니다.

`C-205`는 맞춤 모드의 변형이 아니라 설정 하나로 가져왔습니다(`R-229`). 원본에서는
"너비 맞춤(분할)"이 다섯 번째 맞춤 모드였지만, 여기서 반쪽은 언제나 화면에 통째로
들어가므로 맞춤 모드와 나란히 설 이유가 없습니다.

`C-204`의 확대 상한은 배수 대신 켜고 끄는 스위치 하나로 줄였습니다(`R-2B4`).
통째로 맞춤과 1:1은 애초에 늘리지 않으니 남는 것은 "채울 것이냐 원본을 지킬 것이냐"
하나뿐이고, 늘리지 않기로 하면 보간 방식(`image-rendering`)을 고를 이유도 사라집니다.

`C-304`의 물어보기는 대화상자가 아니라 첫 장 위에 얹히는 줄 하나로 만들었습니다
(`R-2B5`). 원본은 책을 열기 전에 모달로 물었지만, 여기서는 첫 장이 이미 떠 있는
채로 묻고 답을 받기 전까지 그 줄이 사라지지 않습니다 — 읽기 시작한 뒤에 눌러도
그 자리로 갑니다.

`C-306`에서는 목록에서 지우는 것만 남아 있었고, 칸마다 ✕ 하나로 가져왔습니다
(`R-286`). 원본의 별도 북마크 창은 만들지 않았습니다. 격자를 좁힌 것이 이미
목록이고(`R-284`), 거기에 지우는 길만 없었습니다.

`C-203`의 왼쪽·오른쪽 회전은 시계 방향 하나로 줄였습니다. 네 번 누르면 제자리이고,
반대로 도는 것은 세 번입니다. 눕혀 스캔된 책을 한 번 세워 두면 그 각도가 그 책에
남으므로(`R-228`), 자주 누를 버튼이 아닙니다.

원본은 `../cooViewer`입니다. Objective-C 22,161행이고 2005년부터 쌓였습니다.
**코드를 옮기려고 읽는 것이 아니라 동작을 알아내려고** 읽습니다. 그쪽은 3천 행짜리
단일 컨트롤러 두 개에 수동 참조 카운팅이라 옮길 구조가 없습니다.

**읽을 파일**

| 파일                           | 무엇이 있는지                                 |
| ------------------------------ | --------------------------------------------- |
| `en.lproj/Localizable.strings` | 할당 가능한 동작의 전체 목록. 가장 먼저 볼 것 |
| `en.lproj/MainMenu.strings`    | 메뉴와 환경설정의 항목 이름 전체              |
| `CustomImageView.m`            | 맞춤·스크롤·회전·확대 상한 등 화면 쪽 동작    |
| `COImageLoader.m`              | 아카이브·폴더·PDF를 한 권으로 보는 방식       |
| `Controller.h`                 | 기능 표면. 선언만 훑고 구현은 필요할 때만     |

**읽지 말 파일.** `Controller.m`(3,543행), `Controller_input.m`(3,116행),
`PreferenceController.m`(2,884행)은 통째로 읽을 대상이 아닙니다. 이 문서가 짚는
심볼만 `grep`으로 찾아 그 자리만 보세요. Apple Remote 관련 1,511행은 하드웨어가
단종되어 볼 이유가 없습니다.

**각 항목의 세 줄**

- **원본** — cooViewer가 무엇을 하는지와 그 근거
- **지금** — comicyuri가 지금 무엇을 하는지
- **할 일** — 무엇을 만들면 되는지

---

## 3. 책 사이 이동과 기억

이 장은 비었습니다. 책 사이를 오가는 일은 `R-216`으로, 설정을 책마다 기억하는 일은
`R-2B3`으로, `C-304`가 `R-2B5`로, `C-306`이 `R-286`으로 갔습니다. `C-302`와 `C-305`는
문서가 시키는 대로 실제 파일을 세어 보고 가져오지 않기로 했습니다 — 아래 "가져오지
않기로 한 것"에 그 근거가 있습니다.

---

## 4. 새 의존성이 필요한 것

여기부터는 코드 양보다 의존성 결정이 문제입니다. 3장까지가 끝났으므로 이제 그
결정을 내릴 자리인데, 네 항목 모두 근거가 될 파일을 세어 보는 것이 먼저였습니다.
세어 본 결과는 5장에 있습니다 — **넷 다 지금 손에 있는 파일로는 필요가 없습니다.**

**먼저 메운 것.** `C-402`의 할 일이 "`L-609`를 먼저 메우는 편이 낫다"였고, 그것만은
의존성과 무관하게 지금 할 수 있는 일이라 가져왔습니다. `src/zip.ts`에 직접 겨냥한
테스트가 생겼고(`R-205`), `L-609`는 이제 `src/db.ts`만 남습니다. 나중에 암호든
zip64든 이 파일에 손대게 되면, 그때 기댈 바닥이 깔린 셈입니다.

**C-401 · rar · 7z · lzh**

- **원본** XAD 라이브러리로 zip, cbz, rar, cbr, lzh, lha, 7z, sit을 모두 읽는다.
  `COImageLoader.m`의 `+archiveTypes`.
- **지금** zip과 cbz만 읽는다. 플랫폼 내장 압축 해제 스트림을 쓰므로 압축 관련
  의존성이 하나도 없다.
- **할 일** 보류. 브라우저가 정말 못 하는 유일한 영역이고 Rust나 C를 wasm으로
  들일 근거가 되는 유일한 항목이지만, 세어 보니 그런 파일이 하나도 없다. rar는
  원본 압축 해제 코드의 라이선스가 자유롭지 않아서, 하게 되면 libarchive 계열로
  검토가 필요하다.

**C-402 · 암호 걸린 아카이브**

- **원본** 열 때 암호를 묻고, 맞는지 확인하고, 기억한다.
  `Controller.h`의 `askInArchivePassword:`, `XADWrapper.m`의
  `checkAndSetPassword:`.
- **지금** 없다. 암호 걸린 엔트리를 만나면 그 바이트를 그대로 이미지로 넘기므로
  깨진 페이지가 된다.
- **할 일** 보류. wasm 없이도 된다 — AES 암호화 zip은 WebCrypto로 풀 수 있다.
  선행 작업이던 `L-609`는 메웠다. 남은 것은 `src/zip.ts`의 성격 문제다. 지금은
  "중앙 디렉터리를 읽고 잘라 쓰는" 파일이고, 암호 경로가 붙으면 그렇지 않게 된다.

**C-403 · PDF**

- **원본** PDF를 페이지 넘기는 책으로 본다. `COPDFImageRep.m`.
- **지금** 없다.
- **할 일** 보류. wasm이 필요 없고 pdf.js로 충분하지만, 의존성 크기가 지금 프로젝트
  전체보다 크다. 세어 보니 갖고 있는 PDF는 전부 문서이고 만화가 하나도 없다.

**C-404 · 파일 이름 인코딩 — 만들지 않기로 확인됨**

- **원본** 아카이브 안 파일 이름의 인코딩을 추측하려고 UniversalDetector
  프레임워크를 끌어온다. Shift-JIS로 된 이름 때문이다. `XADWrapper.m`의
  `encoding`.
- **지금** 브라우저에 `TextDecoder`가 내장되어 있고, 기본값인 UTF-8로 읽는다.
- **할 일** 만들지 말 것. UTF-8 플래그가 꺼진 이름을 가진 zip이 하나 있었지만 그
  바이트가 UTF-8이었다 — macOS가 만든 zip은 플래그를 세우지 않으면서 UTF-8로
  적는다. 플래그를 믿고 분기하면 오히려 이쪽이 깨진다. 일본어 이름의 cbz를 열었을
  때 이름이 깨지면 그때 `TextDecoder('shift-jis')` 분기를 두면 된다.

---

## 5. 가져오지 않을 것

**세어 보고 접은 것.** 여섯 항목 모두 "실제로 그런 파일을 갖고 있는지 먼저 확인할
것"이 할 일이었고, 확인한 결과가 아니오였습니다. 2026-09-12에 `~/Downloads`,
`~/Documents`, `~/Desktop`을 센 것입니다.

| 항목    | 무엇을 세었나                                                           | 결론                                                               |
| ------- | ----------------------------------------------------------------------- | ------------------------------------------------------------------ |
| `C-302` | 만화 아카이브 여덟(29~555장)의 엔트리 이름에 `/`가 하나도 없다          | 넣지 않는다. 경계 개념은 위치·슬라이더·썸네일을 모두 복잡하게 한다 |
| `C-305` | 같은 여덟의 이름이 모두 `001_`처럼 0으로 채운 일련번호라 이름순이 맞다  | 넣지 않는다. 날짜 정렬이 고칠 어긋남이 없다                        |
| `C-401` | `cbr`·`rar`·`7z`·`lzh`·`lha`·`sit` 파일이 0개. zip이 84개               | 보류. wasm을 들일 근거가 아직 없다                                 |
| `C-402` | zip 62개의 중앙 디렉터리에서 암호 플래그가 선 엔트리가 0개              | 보류. 풀 것이 없다                                                 |
| `C-403` | PDF 28개가 전부 문서다 — 이력서·계약서·설명서·기술서적. 만화가 0개      | 보류. 프로젝트보다 큰 의존성을 아무도 쓰지 않는다                  |
| `C-404` | UTF-8 플래그가 꺼진 이름 7개가 전부 UTF-8 바이트였다 (macOS가 만든 zip) | 만들지 않는다. 플래그를 믿고 분기하면 이쪽이 깨진다                |

세어 본 것이 늘어나면 결론도 달라집니다. 하위 폴더로 화를 나눈 아카이브, 이름순이
틀리는 책, cbr, 암호 걸린 아카이브, 만화 PDF, 이름이 깨지는 일본어 cbz — 그중
하나라도 손에 들어오면 그때 다시 열 항목입니다. 세는 방법은 위 표의 "무엇을
세었나"에 그대로 적혀 있습니다.

| 원본 기능                       | 이유                                                               |
| ------------------------------- | ------------------------------------------------------------------ |
| 키·마우스 완전 재할당, 3개 모드 | 원본 코드의 큰 부분이지만 뷰어의 가치가 아니다. 고정 키맵으로 간다 |
| Apple Remote, Keyspan           | 하드웨어가 단종됐다. 1,511행                                       |
| Finder에서 보기, 휴지통으로     | 브라우저에 파일 시스템이 없다                                      |
| 파일 연결·생성자 변경           | macOS 고유                                                         |
| 전체화면에서 메뉴바 숨김        | macOS 고유                                                         |
| 루페(확대경)                    | 핀치 줌이 같은 일을 더 잘한다                                      |
| `cvbdl` 자체 포맷               | 원본 전용 포맷이다                                                 |
| `CALayer` 사용 여부 설정        | 원본의 성능 회피책이다                                             |
| CoreImage 필터 패널             | CSS 필터로 가능하지만 뷰어의 본론이 아니다                         |

원본에서 좋았던 발상 하나는 남길 만합니다. 두 장이 보이는 동안 **어느 쪽 페이지를
가리켜 동작하는지**를 구분합니다. 원본의 `ViewOriginal(left)`,
`ShowInFinder(right)`처럼 좌우가 갈리는 동작들입니다. 브라우저에서 그에 해당하는
것은 "이 페이지만 원래 크기로", "이 페이지 이미지 저장" 정도입니다.

---

## 부록 A · 원본을 실리콘에서 빌드하려면

comicyuri 작업과는 무관합니다. 원본을 계속 쓰고 싶을 때만 해당합니다.

앱 코드는 arm64로 그냥 빌드됩니다. Xcode 프로젝트에 아키텍처 지정이 없어서 표준
아키텍처를 따르고, 수동 참조 카운팅 코드도 arm64에서 문제없이 컴파일됩니다.

막는 것은 저장소에 함께 들어 있는 프레임워크 두 개입니다.

| 프레임워크          | 현재 상태          | 쓰임                  |
| ------------------- | ------------------ | --------------------- |
| `XADMaster`         | x86_64 단독 Mach-O | 아카이브 전체         |
| `UniversalDetector` | x86_64 단독 Mach-O | 파일 이름 인코딩 추측 |

둘 다 오픈소스라서 arm64로 다시 빌드해 넣으면 됩니다. 배포 대상은 10.8로 잡혀
있으니 함께 올려야 합니다. 재작성이 필요한 일이 아닙니다.
