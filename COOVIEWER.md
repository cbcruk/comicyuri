# cooViewer에서 가져올 것

레퍼런스로 삼은 cooViewer를 읽고, comicyuri에 아직 없는 동작을 할 순서대로 적은
문서입니다.

## 이 문서에 대하여

`SPEC.md`는 **지금 되는 것**을 적습니다. 이 문서는 **아직 없는 것**을 적습니다.
항목을 구현하면 `SPEC.md`로 옮기고 여기서는 지웁니다.

원본은 `../cooViewer`입니다. Objective-C 22,161행이고 2005년부터 쌓였습니다.
**코드를 옮기려고 읽는 것이 아니라 동작을 알아내려고** 읽습니다. 그쪽은 3천 행짜리
단일 컨트롤러 두 개에 수동 참조 카운팅이라 옮길 구조가 없습니다.

**읽을 파일**

| 파일                           | 무엇이 있는지                                 |
| ------------------------------ | --------------------------------------------- |
| `en.lproj/Localizable.strings` | 할당 가능한 동작의 전체 목록. 가장 먼저 볼 것 |
| `en.lproj/MainMenu.strings`    | 메뉴와 환경설정의 항목 이름 전체              |
| `CustomImageView.m`            | 맞춤·스크롤·회전·확대 상한 등 화면 쪽 동작    |
| `COImageLoader.m`              | 아카이브·폴더·PDF를 한 권으로 보는 방식       |
| `Controller.h`                 | 기능 표면. 선언만 훑고 구현은 필요할 때만     |

**읽지 말 파일.** `Controller.m`(3,543행), `Controller_input.m`(3,116행),
`PreferenceController.m`(2,884행)은 통째로 읽을 대상이 아닙니다. 이 문서가 짚는
심볼만 `grep`으로 찾아 그 자리만 보세요. Apple Remote 관련 1,511행은 하드웨어가
단종되어 볼 이유가 없습니다.

**각 항목의 세 줄**

- **원본** — cooViewer가 무엇을 하는지와 그 근거
- **지금** — comicyuri가 지금 무엇을 하는지
- **할 일** — 무엇을 만들면 되는지

---

## 1. 스프레드 묶기

두 장이 잘못 묶이면 그 뒤의 모든 기능이 무의미해집니다. 넓은 페이지를 알아보는
자동 판정은 `SPEC.md`의 `S-121`과 `R-226`으로 들어갔고, 남은 것은 그 판정이
틀렸을 때의 탈출구입니다.

**C-102 · 페이지마다 손으로 묶기를 고칠 수 있다**

- **원본** 자동 판정이 틀렸을 때 지금 페이지의 묶기를 뒤집는다. 책마다
  `"3"`(혼자) 또는 `"4-5"`(짝) 형태로 저장되고, 자동 판정보다 먼저 읽힌다.
  `Controller.m`의 `marksArray`와 `Controller_input.m`의 `switchSingleWithPage`,
  `switchBindWithPage`. 저장 위치는 책 설정의 `marks` 키.
- **지금** 없다. 페이지 크기는 재어 두지만(`S-121`), 그 판정을 뒤집을 방법이
  없다.
- **할 일** 키 하나로 지금 스프레드의 묶기를 뒤집고, 책 진행 상태와 같은 자리에
  저장한다. 자동 판정이 아무리 좋아도 이 탈출구가 없으면 어긋난 책을 못 읽는다.
  스캔본에서는 자동 판정만으로 부족한 경우가 흔하다. 저장할 자리는 C-303과 함께
  정한다.

---

## 2. 읽는 흐름

**C-201 · 스크롤이 끝에 닿으면 페이지가 넘어간다**

- **원본** 너비 맞춤처럼 페이지가 화면보다 클 때 휠이 무엇을 하는지를 네 가지로
  고른다. 스크롤만 하거나, 끝까지 간 뒤 화면 안에서 다음 칸으로 가거나, 끝에서
  페이지를 넘기거나, 항상 넘긴다. 환경설정의 "Normal Scroll",
  "Scroll+TurnPage". `Controller_input.m`의 `wheelAction:`과 `canScrollMode`.
- **지금** 확대된 상태에서만 스크롤이 페이지를 움직이고(`R-240`) 끝에 닿아도
  넘어가지 않는다. 확대하지 않았을 때는 스크롤을 가로채지 않는다.
- **할 일** 세로로 긴 페이지를 너비 맞춤으로 읽을 때 필요하다. 끝에 닿았다는
  판정에 여유 픽셀을 두어야 한다. 소수점 스크롤 위치에서 한 픽셀이 남아 안
  넘어가는 일이 흔하다.

**C-202 · 뒤로 넘기면 앞 페이지의 끝에서 시작한다**

- **원본** 뒤로 넘길 때 앞 페이지의 위가 아니라 아래에 가 있다. 되돌아 읽는
  움직임과 맞는다. `CustomImageView.m`의 `setStartFromEnd:`, 설정은
  `PrevPageMode`.
- **지금** 페이지를 넘기면 줌과 위치가 항상 원점으로 돌아간다(`R-239`).
- **할 일** C-201과 한 쌍이다. 둘 중 하나만 있으면 앞뒤가 어긋난다.

**C-203 · 회전**

- **원본** 왼쪽·오른쪽 90도 회전. 화면 맞춤이 회전된 기준으로 다시 계산된다.
  `CustomImageView.m`의 `rotateRight`, `rotateLeft`, `rotateMode`.
- **지금** 없다.
- **할 일** 눕혀 스캔된 양면 페이지에서 필요하다. 회전 상태를 책마다 기억할지는
  C-303과 함께 정한다.

**C-204 · 확대 상한**

- **원본** 원본보다 몇 배까지만 늘린다. 작은 이미지가 화면을 채우려고 뭉개지는
  것을 막는다. 환경설정의 "Max enlargement:", `Controller.m`의 `maxEnlargement`.
  보간 방식도 따로 고른다.
- **지금** 맞춤 모드가 상한 없이 늘린다.
- **할 일** 상한값 설정 하나와, 늘릴 때의 `image-rendering` 선택. 저해상도
  스캔본에서 체감이 크다.

**C-205 · 넓은 페이지를 반으로 나눠 읽기**

- **원본** 너비 맞춤의 변형으로, 화면보다 넓은 페이지를 좌우 절반씩 차례로
  보여준다. 환경설정의 "Fit to Screen Width(divide)",
  `Controller.m`의 `fitToScreenWidthDivide:`.
- **지금** 없다. 넓은 페이지는 통째로 작게 들어간다.
- **할 일** 세로 화면에서 양면 스캔을 읽는 유일한 방법이다. 넓은 페이지를
  알아보는 일은 `R-226`이 이미 하고 있으므로 그 판정을 그대로 쓴다.

**C-206 · 반 페이지씩, N장씩 넘기기**

- **원본** 반 페이지 앞뒤, 그리고 정해 둔 장수만큼 건너뛰기. `Localizable.strings`의
  `HalfNextPage`, `HalfPreviousPage`, `Skip`, `BackSkip`.
- **지금** 한 스프레드 단위로만 움직인다.
- **할 일** 반 페이지는 C-201이 있으면 대체된다. 건너뛰기는 따로 필요하다.

**C-207 · 페이지 번호와 퍼센트로 이동**

- **원본** 번호를 입력해 가거나 비율로 간다. `Go to Page`, `Go to %`.
- **지금** 슬라이더와 썸네일 격자로만 간다.
- **할 일** 슬라이더가 대부분을 덮는다. 우선순위 낮음.

**C-208 · 책의 끝에서 무엇을 할지**

- **원본** 네 가지다. 같은 책의 반대쪽 끝으로 돌아가거나, 다음 책의 첫 페이지로
  가거나, 다음 책의 마지막 페이지로 가거나, 아무것도 안 한다. 환경설정의
  "Loop :", `Controller_input.m`의 `loopCheck` 분기.
- **지금** 마지막에서 멈춘다.
- **할 일** 원본에서는 이것이 C-301과 같은 장치다. 책의 끝을 넘기는 동작이 곧
  다음 권을 여는 동작이다. C-301을 만들 때 이 네 선택지로 함께 설계할 것. 따로
  만들면 두 기능이 겹친다.

**C-209 · 페이지 번호 표시**

- **원본** 스프레드 번호, 전체 장수, 그리고 파일 이름까지 한 줄로 보여준다.
  형식은 `#1-2/345 (67.jpg | 89.jpg)`. 자동 숨김과 글꼴·색 설정이 있다.
  환경설정의 "Page Number", `Controller.m`의 `setPageTextField`.
- **지금** 슬라이더가 위치를 보여준다.
- **할 일** 파일 이름이 붙는 것이 핵심이다. 정렬이 이상할 때 원인을 바로 알 수
  있다. 파일 이름은 이미 `Page.name`에 있다.

**C-210 · 슬라이드쇼**

- **원본** 간격을 정해 자동으로 넘긴다. 시작·정지가 따로 있다.
  환경설정의 "Slideshow Delay".
- **지금** 없다.
- **할 일** 우선순위 낮음. 넣을 때 툴바 자동 숨김(`R-251`)과의 관계를 정해야
  한다.

---

## 3. 책 사이 이동과 기억

**C-301 · 책장으로 돌아가지 않고 다음 책으로**

- **원본** 같은 폴더의 다음 아카이브를 그 자리에서 연다. 마지막 페이지에서 계속
  넘기면 다음 권으로 이어진다. `NextFolder(Archive)`,
  `PreviousFolder(Archive)`, `Controller.h`의 `openFromSameDir:last:`. 첫
  페이지에서 열지 마지막 페이지에서 열지도 고른다.
- **지금** 리더에서 나가려면 책장으로 돌아가야 한다.
- **할 일** 브라우저에는 파일 시스템의 폴더가 없지만 책장이 같은 역할을 한다.
  "책장 순서상 다음 책"으로 정의하면 된다. 연속으로 읽을 때 체감이 가장 큰
  항목이다.

**C-302 · 아카이브 안의 하위 폴더를 넘나든다**

- **원본** 한 아카이브에 여러 화가 폴더로 나뉘어 들어 있을 때 그 경계를 알고
  넘나든다. 환경설정의 "Read nested folders", `COImageLoader.m`의
  `nextFolder:`, `prevFolder:`, `readSubFolder`.
- **지금** 아카이브의 모든 이미지를 한 줄로 펴서 한 권으로 본다.
- **할 일** 실제로 그런 아카이브를 갖고 있는지 먼저 확인할 것. 아니라면 넣지
  말 것. 경계 개념이 들어오면 위치·슬라이더·썸네일이 모두 복잡해진다.

**C-303 · 설정을 책마다 기억한다**

- **원본** 방향, 한 장/두 장, 맞춤, 묶기 교정을 책마다 저장한다. 전역 기본값과
  따로 산다. 환경설정의 "Remember chenged book setting of all books",
  `Controller.h`의 `rememberBookSettings`, `currentBookSetting`.
  설정이 없는 책에는 형제 책의 설정을 쓸지 물어본다.
- **지금** 설정은 전역이고(`P-303`) 책마다 남는 것은 위치와 북마크뿐(`P-302`)이다.
- **할 일** C-102의 묶기 교정이 저장될 자리가 필요해서 어차피 해야 한다. 전역
  기본값에 책별 덮어쓰기를 얹는 모양으로 두 층을 만든다.

**C-304 · 이어 읽기를 물어볼지 정한다**

- **원본** 저장된 위치가 있을 때 조용히 그리로 가거나, 물어보거나, 항상 처음부터
  본다. `Do you want to go to %i page?`, `Controller.h`의 `goToLastPageMode`.
- **지금** 언제나 조용히 저장된 위치로 간다(`N-403`).
- **할 일** 설정 하나. 우선순위 낮음.

**C-305 · 페이지 정렬**

- **원본** 이름, 만든 날짜, 고친 날짜, 무작위 넷 중에서 고르고 읽는 중에도
  바꾼다. `ChangeSortMode`, `COImageLoader.h`의 `canSortByDate`.
- **지금** 이름순 고정이다. `Intl.Collator`에 `numeric: true`를 줘서 자연
  정렬은 이미 맞다.
- **할 일** 날짜 정렬은 zip 엔트리의 날짜가 있어야 한다. `src/zip.ts`가 중앙
  디렉터리를 이미 읽으므로 거기서 꺼낼 수 있다. 다만 이름순이 틀린 아카이브가
  실제로 있었는지 먼저 볼 것.

**C-306 · 북마크 목록**

- **원본** 북마크를 목록으로 보고 편집하고, 앞뒤 북마크로 이동한다.
  `BookmarkController.m`, `NextBookmark`, `PreviousBookmark`.
- **지금** 페이지마다 켜고 끌 수 있을 뿐 목록이 없다. `L-603`과 같은 항목이다.
- **할 일** 앞뒤 북마크 이동만 먼저 넣어도 대부분 해결된다.

---

## 4. 새 의존성이 필요한 것

여기부터는 코드 양보다 의존성 결정이 문제입니다. 위의 항목들을 다 하기 전에는
손대지 않는 쪽을 권합니다.

**C-401 · rar · 7z · lzh**

- **원본** XAD 라이브러리로 zip, cbz, rar, cbr, lzh, lha, 7z, sit을 모두 읽는다.
  `COImageLoader.m`의 `+archiveTypes`.
- **지금** zip과 cbz만 읽는다. 플랫폼 내장 압축 해제 스트림을 쓰므로 압축 관련
  의존성이 하나도 없다.
- **할 일** 브라우저가 정말 못 하는 유일한 영역이고, Rust나 C를 wasm으로 들일
  근거가 되는 유일한 항목이다. rar는 원본 압축 해제 코드의 라이선스가 자유롭지
  않으니 libarchive 계열로 검토가 필요하다. 갖고 있는 파일에 cbr이 얼마나 되는지
  세어 보고 결정할 것.

**C-402 · 암호 걸린 아카이브**

- **원본** 열 때 암호를 묻고, 맞는지 확인하고, 기억한다.
  `Controller.h`의 `askInArchivePassword:`, `XADWrapper.m`의
  `checkAndSetPassword:`.
- **지금** 없다.
- **할 일** wasm 없이도 된다. AES 암호화 zip은 WebCrypto로 풀 수 있다. 다만
  `src/zip.ts`가 직접 구현이라 여기에 암호 경로가 붙으면 그 파일의 성격이
  바뀐다. `L-609`(zip 계층 직접 테스트 없음)를 먼저 메우는 편이 낫다.

**C-403 · PDF**

- **원본** PDF를 페이지 넘기는 책으로 본다. `COPDFImageRep.m`.
- **지금** 없다.
- **할 일** wasm이 필요 없다. pdf.js로 충분하다. 다만 의존성 크기가 지금 프로젝트
  전체보다 크다.

**C-404 · 파일 이름 인코딩 — 이미 해결됨**

- **원본** 아카이브 안 파일 이름의 인코딩을 추측하려고 UniversalDetector
  프레임워크를 끌어온다. Shift-JIS로 된 이름 때문이다. `XADWrapper.m`의
  `encoding`.
- **지금** 브라우저에 `TextDecoder`가 내장되어 있다.
- **할 일** 만들지 말 것. zip 엔트리의 UTF-8 플래그가 꺼져 있을 때
  `TextDecoder('shift-jis')`로 다시 디코딩하는 분기만 `src/zip.ts`에 두면 된다.
  일본어 이름의 cbz를 열었을 때 이름이 깨지면 이것이 원인이다.

---

## 5. 가져오지 않을 것

| 원본 기능                       | 이유                                                               |
| ------------------------------- | ------------------------------------------------------------------ |
| 키·마우스 완전 재할당, 3개 모드 | 원본 코드의 큰 부분이지만 뷰어의 가치가 아니다. 고정 키맵으로 간다 |
| Apple Remote, Keyspan           | 하드웨어가 단종됐다. 1,511행                                       |
| Finder에서 보기, 휴지통으로     | 브라우저에 파일 시스템이 없다                                      |
| 파일 연결·생성자 변경           | macOS 고유                                                         |
| 전체화면에서 메뉴바 숨김        | macOS 고유                                                         |
| 루페(확대경)                    | 핀치 줌이 같은 일을 더 잘한다                                      |
| `cvbdl` 자체 포맷               | 원본 전용 포맷이다                                                 |
| `CALayer` 사용 여부 설정        | 원본의 성능 회피책이다                                             |
| CoreImage 필터 패널             | CSS 필터로 가능하지만 뷰어의 본론이 아니다                         |

원본에서 좋았던 발상 하나는 남길 만합니다. 두 장이 보이는 동안 **어느 쪽 페이지를
가리켜 동작하는지**를 구분합니다. 원본의 `ViewOriginal(left)`,
`ShowInFinder(right)`처럼 좌우가 갈리는 동작들입니다. 브라우저에서 그에 해당하는
것은 "이 페이지만 원래 크기로", "이 페이지 이미지 저장" 정도입니다.

---

## 부록 A · 원본을 실리콘에서 빌드하려면

comicyuri 작업과는 무관합니다. 원본을 계속 쓰고 싶을 때만 해당합니다.

앱 코드는 arm64로 그냥 빌드됩니다. Xcode 프로젝트에 아키텍처 지정이 없어서 표준
아키텍처를 따르고, 수동 참조 카운팅 코드도 arm64에서 문제없이 컴파일됩니다.

막는 것은 저장소에 함께 들어 있는 프레임워크 두 개입니다.

| 프레임워크          | 현재 상태          | 쓰임                  |
| ------------------- | ------------------ | --------------------- |
| `XADMaster`         | x86_64 단독 Mach-O | 아카이브 전체         |
| `UniversalDetector` | x86_64 단독 Mach-O | 파일 이름 인코딩 추측 |

둘 다 오픈소스라서 arm64로 다시 빌드해 넣으면 됩니다. 배포 대상은 10.8로 잡혀
있으니 함께 올려야 합니다. 재작성이 필요한 일이 아닙니다.
